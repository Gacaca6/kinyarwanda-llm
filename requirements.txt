# Kinyarwanda LLM — Python dependencies
# Pin versions for reproducibility (CLAUDE.md §5).
# Verified-installed pins are exact; others are minimum-version floors until the
# training environment is built, then run `pip freeze` and replace with exact ==.

# Tokenizer training + inference (byte-level BPE). Verified installed: 0.23.1
tokenizers==0.23.1

# Model + training (Phase 4). Heavy / platform-specific wheel — install per the
# official selector at https://pytorch.org ; floor only until env is frozen.
torch>=2.2

# Wikitext parsing for the Kinyarwanda Wikipedia reader (data/prepare_wikipedia.py).
mwparserfromhell>=0.6

# GPT-2 baseline for the tokenizer fertility comparison (tokenizer/compare_tokenizers.py,
# tokenizer/sweep_tokenizer.py). Cross-platform replacement for the book's offline encoder.
tiktoken>=0.7

# Hugging Face Hub access. NB: the MADLAD-400 reader (data/prepare_madlad.py)
# pulls the raw .jsonl.gz shard directly via huggingface_hub / plain URL, because
# `datasets` >= 4 dropped loading-script support and MADLAD ships a script.
huggingface_hub>=0.23

# Numerics, progress bars, dataset access, regex pretokenization.
numpy>=1.26
tqdm>=4.66
datasets>=2.18
regex>=2024.4
